/// <reference types="node" />
import { Options } from 'execa';
export declare const shouldForceColor: (env?: NodeJS.ProcessEnv) => boolean;
export declare type ShellOptions = Options;
/**
 * Run shell command.
 *
 * @param {string} cmd
 * @param {object} opts
 *
 * @returns {Promise}
 */
export declare const shell: (cmd: string | string[] | TemplateStringsArray, opts?: Partial<ShellOptions>) => Promise<any>;
/**
 * Run shell command.
 *
 * @param {string} cmd
 * @param {object} opts
 *
 * @returns {Promise}
 */
export declare const run: (cmd: string, opts?: Partial<ShellOptions>) => Promise<any>;
