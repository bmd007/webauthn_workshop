export declare type PineFile = Record<string, any>;
export declare type PineFileInfo = {
    file: string;
    ext: string;
    dirname: string;
    pineFile: PineFile;
};
/**
 * Parse Pinefile to object with valid key and value.
 *
 * Will convert
 * - 'b:c' keys to object { b: { c: { _: '' } } }
 * - 'b' keys to object { b: { _: '' } }
 *
 * @param {string} pineFile
 * @param {string} sep
 *
 * @returns {object}
 */
export declare const parsePineFile: (pineFile: PineFile, sep?: string) => PineFile;
export declare const isFile: (filePath: string) => boolean;
export declare const findFile: (file?: string) => string;
export declare const loadPineFile: (input: string) => PineFileInfo;
export declare const findGlobalFile: () => string;
